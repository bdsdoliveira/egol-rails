
<div class="row">
  <div class="span2 page-title">
    Sign up »
    <% if @user.errors.any? %>
      <div class="alert alert-error">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>
        <ul>
          <% @user.errors.full_messages.each do |msg| %>
            <li><%= msg %>.</li>
          <% end %>
        </ul>
      </div>
    <% end %>
  </div>
  <div class="span10">
    <br />
    <%= render 'form' %>
  </div>
</div>

